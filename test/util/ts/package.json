{
  "name": "supastate-test-utils",
  "version": "1.0.0",
  "main": "lib/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rimraf lib",
    "prebuild": "npm run clean",
    "build": "tsc --project tsconfig.json",
    "buildIncremental": "tsc --incremental --project tsconfig.json"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
